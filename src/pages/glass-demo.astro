---
import Layout from '../layouts/Layout.astro';
import GlassPanel from '../components/GlassPanel.astro';
import GlassCard from '../components/GlassCard.astro';

const title = 'Glassmorphism UI Kit - Demostración';
const description = 'Ejemplos y componentes del kit de UI para efectos de glassmorfismo';
---

<Layout title={title} description={description}>
  <div class="container">
    <h1>Glassmorphism UI Kit</h1>
    <p class="intro">
      Este es nuestro kit de componentes para efectos de glassmorfismo. Puedes usar estos
      componentes reutilizables para crear interfaces elegantes con efecto de cristal sin los
      problemas de rendimiento típicos del backdrop-filter.
    </p>

    <section>
      <h2>Paneles de vidrio (GlassPanel)</h2>
      <div class="demo-grid">
        <div class="demo-item">
          <h3>Intensidad: Light</h3>
          <GlassPanel intensity="light" border={true} rounded="md" shadow="sm">
            <div class="panel-content">Panel con efecto de vidrio ligero</div>
          </GlassPanel>
        </div>
        <div class="demo-item">
          <h3>Intensidad: Medium</h3>
          <GlassPanel intensity="medium" border={true} rounded="md" shadow="md">
            <div class="panel-content">Panel con efecto de vidrio medio</div>
          </GlassPanel>
        </div>
        <div class="demo-item">
          <h3>Intensidad: Strong</h3>
          <GlassPanel intensity="strong" border={true} rounded="md" shadow="lg">
            <div class="panel-content">Panel con efecto de vidrio intenso</div>
          </GlassPanel>
        </div>
      </div>
    </section>

    <section>
      <h2>Tarjetas de vidrio (GlassCard)</h2>
      <div class="demo-grid">
        <div class="demo-item">
          <GlassCard title="Tarjeta Básica" subtitle="Con efecto hover" intensity="light">
            <p>Esta tarjeta tiene un efecto de elevación al pasar el cursor</p>
          </GlassCard>
        </div>
        <div class="demo-item">
          <GlassCard
            title="Tarjeta Avanzada"
            subtitle="Sin efecto hover"
            intensity="medium"
            hasHover={false}
          >
            <p>Esta tarjeta no se eleva al pasar el cursor</p>
          </GlassCard>
        </div>
        <div class="demo-item">
          <GlassCard
            title="Tarjeta Máxima"
            subtitle="Con bordes redondeados grandes"
            intensity="strong"
            rounded="lg"
            shadow="lg"
          >
            <p>Tarjeta con efecto de vidrio intenso y sombra grande</p>
          </GlassCard>
        </div>
      </div>
    </section>

    <section>
      <h2>Personalización</h2>
      <p>
        Puedes personalizar fácilmente todos los aspectos del efecto glassmorphism modificando las
        variables CSS en Layout.astro:
      </p>

      <div class="code-example">
        <pre><code>{`
:root {
  --glass-blur-light: 5px;
  --glass-blur-medium: 10px;
  --glass-blur-strong: 15px;
  --glass-bg-light: rgba(255, 255, 255, 0.1);
  --glass-bg-medium: rgba(255, 255, 255, 0.15);
  --glass-bg-strong: rgba(255, 255, 255, 0.25);
  --glass-border-color: rgba(255, 255, 255, 0.2);
  --glass-shadow-color: rgba(0, 0, 0, 0.12);
}`}</code></pre>
      </div>
    </section>

    <section>
      <h2>Versión alternativa (sin backdrop-filter)</h2>
      <p>
        Para dispositivos con problemas de rendimiento, se incluye una versión de respaldo que no
        usa backdrop-filter. Esta versión se activa automáticamente cuando backdrop-filter no está
        disponible.
      </p>

      <div class="demo-grid">
        <div class="demo-item">
          <GlassPanel intensity="medium" border={true} class="no-backdrop">
            <div class="panel-content">Versión sin backdrop-filter</div>
          </GlassPanel>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #333;
    text-align: center;
    margin-top: 2rem;
  }

  h2 {
    font-size: 1.8rem;
    margin: 2rem 0 1rem;
    border-bottom: 2px solid var(--glass-border-color, rgba(255, 255, 255, 0.2));
    padding-bottom: 0.5rem;
  }

  h3 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }

  .intro {
    font-size: 1.1rem;
    max-width: 800px;
    margin: 0 auto 2rem;
    text-align: center;
    line-height: 1.5;
  }

  .demo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .demo-item {
    display: flex;
    flex-direction: column;
    min-height: 180px;
  }

  .panel-content {
    padding: 1.5rem;
    text-align: center;
  }

  .code-example {
    margin: 1rem 0;
    overflow-x: auto;
  }

  pre {
    background-color: rgba(30, 30, 30, 0.9);
    color: #eaeaea;
    padding: 1rem;
    border-radius: 0.5rem;
    font-family: monospace;
    font-size: 0.9rem;
    line-height: 1.5;
    overflow-x: auto;
  }

  .no-backdrop {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    background-color: rgba(255, 255, 255, 0.7) !important;
  }

  /* Tema oscuro */
  :global(html[data-theme='dark']) h1,
  :global(html[data-theme='dark']) h2,
  :global(html[data-theme='dark']) h3 {
    color: #f0f0f0;
  }

  :global(html[data-theme='dark']) .no-backdrop {
    background-color: rgba(30, 30, 30, 0.7) !important;
  }

  /* Móvil */
  @media (max-width: 768px) {
    h1 {
      font-size: 2rem;
      margin-top: 1rem;
    }

    h2 {
      font-size: 1.5rem;
    }

    .demo-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
