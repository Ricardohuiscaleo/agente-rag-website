---
// Importaciones necesarias
import Chatbot from './Chatbot';

// Props que el componente puede recibir
interface Props {
  title?: string;
  description?: string;
  icon?: string;
  color?: string;
}

// Valores por defecto de las props
const {
  title = 'Asistente Virtual',
  description = 'Chatbot inteligente que responde preguntas basadas en tus datos',
  icon = '💬',
  color = '#4079ff',
} = Astro.props;
---

<!-- Renderizamos el componente React con client:load para que se ejecute en el cliente -->
<Chatbot client:load title={title} description={description} icon={icon} color={color} />
